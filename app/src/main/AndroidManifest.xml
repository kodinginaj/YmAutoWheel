<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ymautowheel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Ubah_stocksuspensi"></activity>
        <activity android:name=".Tambah_stocksuspensi" />
        <activity android:name=".Ubah_stockban" />
        <activity android:name=".notification" />
        <activity android:name=".Notification" />
        <activity android:name=".Informasi_stoksuspensi" />
        <activity android:name=".SuspensiActivity" />
        <activity android:name=".Tambah_stockban" />
        <activity android:name=".Informasi_stokban" />
        <activity android:name=".BanActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
    </application>

</manifest>